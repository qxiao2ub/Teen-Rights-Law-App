# Supplied UI Design Reference

The user-supplied `legal-counsel-source.zip` was used as the visual and interaction reference for the Streamlit migration. The runtime implementation lives in `streamlit_app.py`; the GitHub/Streamlit deployment does not need the original Node/TanStack project.

See `docs/UI_MIGRATION.md` for the design elements reproduced in Streamlit.
